@{
    ViewData["Title"] = "Game";
}
@model GameViewModel
<h1>@ViewData["Title"]</h1>

<p>Current Players:</p>
<ol>
    @foreach (var player in Model.Players)
    {
        <li>@(player.FirstName) @(player.LastName)</li>
    }
</ol>
<a asp-action="EditPlayers">Edit/Add players</a>